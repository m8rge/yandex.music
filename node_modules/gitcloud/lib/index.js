'use strict';

require('source-map-support/register');

var gitCloud = require('./gitCloud');

function main(pageUrl) {
    return gitCloud(pageUrl);
}

module.exports = main;
//# sourceMappingURL=index.js.map