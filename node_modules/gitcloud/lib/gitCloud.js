'use strict';

var _axios = require('axios');

var _axios2 = _interopRequireDefault(_axios);

var _cheerio = require('cheerio');

var _cheerio2 = _interopRequireDefault(_cheerio);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function gitCloud(pageUrl) {
    return _axios2.default.get(pageUrl).then(function (response) {
        var dom = response.data;
        var $ = _cheerio2.default.load(dom);

        var fileIndex = $('#file-index').find('a').map(function (index, element) {
            var elementJq = $(element);
            return {
                name: elementJq.text(),
                url: elementJq.attr('href')
            };
        }).get();

        return fileIndex;
    });
}

module.exports = gitCloud;
//# sourceMappingURL=gitCloud.js.map